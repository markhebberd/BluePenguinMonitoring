/**
 * When the breeding-detection explanation was last checked against the code.
 *
 * Maintained by scripts/check-algorithm-doc.mjs — don't edit by hand. The deploy
 * workflow re-fingerprints the algorithm's source and fails if it no longer matches
 * `fingerprint`, so this date is a real claim: on that day a person read the code and
 * the explanation together and confirmed the explanation was true.
 */
export const ALGORITHM_DOC = {
  fingerprint: 'be2266bc4363d25b',
  reviewed: '2026-07-30',
};
